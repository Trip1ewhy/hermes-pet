Working for Hermes 桌宠（hermes-pet 仓库）。

工作约定：

1. 所有设计讨论结果更新到 `docs/` 下的 markdown 文件（`docs/PRD.md` / `docs/tech.md` / `docs/UI-UX-Style.md` / `docs/features/*.md`）
2. 代码改动按正常 git 流程走（feature branch → commit → PR / push）
3. 不要在仓库之外创建或修改文件，除非用户明确要求（例如 `~/.hermes/`、Tauri 应用 bundle 等运行时路径）
4. 文档语言中文为主，技术术语和 API 名保英文
5. 任何"待定 / TBD"在写代码前必须回头跟用户确认
6. README.md 是对外的索引，PRD.md 是对内的产品需求；两者重叠部分以 README 为准
